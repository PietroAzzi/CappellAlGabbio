doctype html
html
    head
        title Lego con i Pirati
        link(rel="stylesheet", href="/css/style.css")
        meta(name="viewport" content="width=device-width, initial-scale=1")
    body
        header.header
            h1 Pirati!
        article.container
            each LEGO in vettoreJSON
                div
                img(src=`/img/${LEGO.Copertina}`)
                a(href=`/istruzioni?id=${LEGO.SetNumber}`) Ti piace ? Scopri come costruirlo!